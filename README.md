# www.barbiche.github.io

This repository contains my website. It's made using [jekyll](https://jekyllrb.com/).

To run locally:

```
bundle install
bundle exec jekyll serve
```

Then open localhost:4000